DATABASE_URL="postgresql://ssdomada:ssdomada@localhost:5432/ssdomada"
NEXTAUTH_SECRET="wiA72ps5SzKcFIb6ThvIqakwoBYN4TjUfHcqvbWlKD0="
NEXT_PUBLIC_APP_URL="https://ssdomada.site"
# Optional: HTTPS origin Omada uses for External Portal URL (defaults to NEXT_PUBLIC_APP_URL)
#OMADA_PORTAL_PUBLIC_BASE_URL="https://ssdomada.site"
NEXTAUTH_URL="https://ssdomada.site"
CRON_SECRET="<generate random hex, e.g. openssl rand -hex 32>"

# Omada Controller
OMADA_URL="https://server.ssdomada.site"
OMADA_CONTROLLER_ID="214e8438096716894ee88534b9c67f17"
OMADA_CLIENT_ID="f2745c9d60e34a37abae6870f65ff60e"
OMADA_CLIENT_SECRET="256c27a0b578428a90a250224050be5a"
OMADA_SITE_REGION="Tanzania"
OMADA_SITE_TIME_ZONE="Africa/Nairobi"
OMADA_SITE_SCENARIO="General"
OMADA_DEVICE_USERNAME="ssdomada"
OMADA_DEVICE_PASSWORD="<10-64 chars with upper/lower/number/symbol>"
# Resellers can send per-device username/password from the dashboard when adopting; that overrides these for that request only.
# If v1 adopt returns 404, set true to also POST /openapi/v2/.../adopt (controller-dependent).
#OMADA_ADOPT_TRY_V2="true"

# Redis (optional — defaults to redis://localhost:6379; in-memory fallback if unreachable)
#REDIS_URL="redis://localhost:6379"

# ============================================================
# Snippe Payment Gateway  (API version 2026-01-25)
# Docs: https://docs.snippe.sh/docs/2026-01-25
# ============================================================
SNIPPE_BASE_URL="https://api.snippe.sh"
# Bearer API key — Dashboard → Settings → API Keys
SNIPPE_API_KEY="snp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
# Signing key for webhook signature verification — Dashboard → Settings → Webhook Secret
# (or POST /api/v1/settings/webhook-secret/regenerate)
SNIPPE_WEBHOOK_SECRET="whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
# Legacy aliases (kept so older deploys do not break — prefer SNIPPE_BASE_URL above)
# SNIPPE_API_URL="https://api.snippe.sh"

# FreeRADIUS
RADIUS_SECRET="SSDomada2026SecureR@dius"
RADIUS_NAS_IP="107.152.36.210"

# Omada "Pre-Authentication Access" — set in Controller UI (Portal → Access Control); SSDomada does not push this via API yet.
# Hostnames to allow before login: your app + Snippe + OS captive-detection probes. Evidence + full list: docs/captive-preauth-allowlist.md
#
# Example CSV (for your runbook only — uncomment if you use a script later):
# OMADA_PREAUTH_REFERENCE_HOSTS="captive.apple.com,www.apple.com,connectivitycheck.gstatic.com,clients3.google.com,www.msftconnecttest.com,dns.msftncsi.com"
# OMADA_PREAUTH_YOUR_APP="ssdomada.site,api.snippe.co.tz"